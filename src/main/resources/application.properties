# Server port
server.port=8080

# Application name
spring.application.name=demo

# Project version (should match the version in pom.xml)
project.version=0.0.1-SNAPSHOT

# Logging configuration
logging.level.root=INFO
logging.level.com.example.demo=DEBUG
